{
  "name": "field-survey-pro",
  "version": "1.0.0",
  "description": "Mobile-first app for estimators and installers to organize job site photos and notes",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "build": "npm run build --workspace=client && npm run build --workspace=server"
  },
  "keywords": ["field-survey", "photos", "mobile", "pwa"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
